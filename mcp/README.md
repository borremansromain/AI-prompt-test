# Stock API MCP server

Install dependencies with `npm install`, then set `BACKEND_URL` and `MCP_JWT_TOKEN` when protected API tools are needed.

Run with `npm start`. The server exposes health, category, and product tools over stdio.